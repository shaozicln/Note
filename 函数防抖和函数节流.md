# 函数防抖和函数节流

#### 防抖和节流的核心就是定时器

## 函数防抖
函数防抖，就是指触发一个事件，在 n 秒内这个事件不会重复执行，n 秒内执行了，则重新计时。
##### 函数防抖就是法师发技能的时候要读条，技能读条没完再按技能就会重新读条。

## 函数节流
规定在一个单位时间内，只能触发一次函数。如果这个单位时间内触发多次函数，只有一次生效。
##### 函数节流就是fps游戏的射速，就算一直按着鼠标射击，也只会在规定射速内射出子弹。